<style>
  .article {
    grid-area: 2 / 1 / 3 / 5;
	  padding: 64px 0 0 48px;
  }

  .title {
    font-family: var(--body-text);
    color: var(--primary-light);
    font-weight: 700;
    font-size: 2rem;
    line-height: 2.25rem;
    letter-spacing: 0.075rem;
    margin: 0;
    padding: 1rem 0 0.25rem;
    max-width: 280px;
  }

  .line {
    width: 4000px;
    background-color: var(--primary-dark);
    height: 1px;
    position: relative;
    left: -50%;
    margin: 1rem 0;
    z-index: -1;
  }

  .category {
    border-radius: 2px 2px 12px 2px;
    color: var(--primary-dark);
    font-family: var(--body-text);
    text-transform: uppercase;
    font-weight: 700;
    font-size: 0.75rem;
    letter-spacing: 0.02rem;
    text-align: center;
    padding: 4px 16px 4px 12px;
    position: relative;
  }

  .redesign {
    background-color: var(--accent-secondary);
  }

  .date {
    color: var(--accent-primary);
    font-family: var(--body-text);
    font-size: 0.75rem;
    font-weight: 400;
    line-height: 1rem;
    letter-spacing: 0.06rem;
    margin-top: 4px;
    position: relative;
    left: 1rem;
  }

  p {
    font-size: 1.25rem;
    line-height: 1.5rem;
    font-family: var(--body-text);
    font-weight: 400;
    color: var(--primary-light);
    letter-spacing: 0.075rem;
    max-width: 270px;
  }

  .bold {
    font-weight: 700;
  }

  .text {
    grid-area: 3 / 1 / 4 / 5;
    padding: 48px 0 0 48px;
  }

  .inline-image {
    width: 100%;
    position: relative;
    left: -24px;
    box-shadow:
    0px 4px 8px rgba(0, 0, 0, 0.04),
    0px 20px 12px -12px rgba(0, 0, 0, 0.12);
    border-radius: 24px 4px 4px 4px;
  }

  @media screen and (min-width: 1048px) {
    .article {
        grid-area: 2 / 2 / 3 / 8;
        max-width: 542px;
        padding: 64px 48px 0;
      }

    .title {
      max-width: 448px;
      padding: 4rem 0 0.5rem;
    }

    p {
    max-width: 520px;
  }

  .line {
    left: -1900px;
  }

  .text {
    grid-area: 3 / 2 / 4 / 8;
  }

}
</style>

<div class="article">
  <h1 class="title">
      When push comes to shove, build a new site.
  </h1>
  <div class="line"></div>

  <span class="category redesign">REDESIGN</span>
  <span class="date">1st October</span>

</div>
<div class="text">
  <p>It's been one hell of a week. But sometimes the only way to get on with that one thing you have been putting off for a while, which you just can't quite
    push yourself to do, is to get shoved. They're <span class="bold">unexpected</span>. They're <span class="bold">mentally draining</span>. And it can cause pain.
  </p>
  <p>
    But on the flip side, this week has shoved myself so hard, that in three nights i've designed, coding and populated this new site. Which quite frankly I didn't think
    was achieveable. The code is definitely spaghetti in some places, the design needs some tweaks - and I actually need to hook up the components & CMS properly (whoops) - but
    i've got this starting point.
  </p>
  <p>
    Over the next few days, weeks and months I will noodle away and iterate - whilst hopefully keeping a log of it all here in writing.
  </p>
  <p>
    Just remember, when you get shoved into a corner, then you discover you're real worth and the people around you that you can rely on. It can become positive, if you let it.
  </p>
</div>


